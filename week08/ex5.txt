Physical address is in physical memory.

The virtual memory is  an memory generated by procceses.

The virtual memory stores the pieces of data(pages) and  they mapped onto physical memory.

The virtual memory addresses number can be bigger than physical memory addresses number, because not all data of proccess in the memory in the same time.